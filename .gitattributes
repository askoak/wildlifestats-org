# Shell scripts must use LF so they run on the Linux CI runner
# (a CRLF script fails with `bash\r: No such file or directory`).
*.sh text eol=lf

# Keep web source text normalized
*.html text
*.css text
*.js text
*.json text
*.xml text
*.toml text
*.md text
