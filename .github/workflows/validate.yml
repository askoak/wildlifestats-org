name: validate

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  brwc-content-guard:
    name: BRWC content guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for forbidden strings
        run: bash scripts/check-no-brwc.sh

  credentials-leakage-guard:
    name: Credentials leakage guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for bearer tokens and API keys
        run: bash scripts/check-no-credentials.sh

  common-gates-test:
    name: _common discipline gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run _common gate tests
        run: PYTHONPATH=. python wildlifestats/_pipeline/_common/test_gates.py

  flyway-triggers-test:
    name: Flyway trigger engine tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run Flyway 4.5+h trigger tests
        run: PYTHONPATH=. python wildlifestats/_pipeline/flyway/test_triggers.py

  link-check:
    name: Internal link check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install link checker
        run: npm install -g linkinator@6
      - name: Check internal links
        run: |
          linkinator . \
            --recurse \
            --skip "^https?://" \
            --skip "^mailto:" \
            --skip "node_modules" \
            --skip ".git" \
            --skip "docs/handoff"

  html-validate:
    name: HTML validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install html-validate
        run: npm install -g html-validate@8
      - name: Validate HTML files
        run: |
          html-validate \
            --config .htmlvalidate.json \
            $(find . -name "*.html" -not -path "./node_modules/*" -not -path "./.git/*")

  cube-validate:
    name: Synthetic cube validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate committed cube
        run: python wildlifestats/_build/validate_cube.py
